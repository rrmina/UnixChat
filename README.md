# UnixChat

Chat Application for Ubuntu written in C

## How to build
  ```
  make
  ```

## How to run
Run the server first
  ```
  ./bin/server
  ```
and then the client
  ```
  ./bin/client localhost
  ```